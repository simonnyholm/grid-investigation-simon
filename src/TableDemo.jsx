import { useTable } from "react-table";

const TableDemo = () => {



    const tableInstance = useTable({ columns: [], data: [] });
    //tableInstance.rows
    return (
        <div>demo</div>
    );
}

export default TableDemo;